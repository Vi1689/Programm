#include <iostream>
#include <string>
#include <vector>

int main()
{
    std::vector<std::string> sarr;
    std::string s;
    while (std::cin >> s) {
        sarr.push_back(s);
    }

    

    return 0;
}