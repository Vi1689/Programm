#include <iostream>
#include <stack>
#include <vector>

int main()
{
    int n;
    size_t akk = 0;
    std::cin >> n;
    std::vector<size_t> row_rectangles;
    while (n--) {
        size_t a;
        std::cin >> a;
        row_rectangles.push_back(a);
    }

    for (const auto& i : row_rectangles) {
    }

    return 0;
}